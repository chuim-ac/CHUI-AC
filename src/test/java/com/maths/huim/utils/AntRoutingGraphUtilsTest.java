package com.maths.huim.utils;

import com.maths.huim.models.AntRoutingGraph;
//import org.junit.*;
import java.util.Arrays;

public class AntRoutingGraphUtilsTest {

//    @Test
//    public void insert() {
//
//        AntRoutingGraph antRoutingGraph = new AntRoutingGraph();
//        AntRoutingGraphUtils antRoutingGraphUtils = new AntRoutingGraphUtils();
//
//        antRoutingGraphUtils.insert(antRoutingGraph, Arrays.asList('t', 'h', 'e'));
//        antRoutingGraphUtils.insert(antRoutingGraph, Arrays.asList('a'));
//        antRoutingGraphUtils.insert(antRoutingGraph, Arrays.asList( 't', 'h', 'e', 'r', 'e'));
//        antRoutingGraphUtils.insert(antRoutingGraph, Arrays.asList( 'a', 'n', 's', 'w', 'e', 'r'));
//        antRoutingGraphUtils.insert(antRoutingGraph, Arrays.asList('a', 'n', 'y'));
//        antRoutingGraphUtils.insert(antRoutingGraph, Arrays.asList('b', 'y'));
//        antRoutingGraphUtils.insert(antRoutingGraph, Arrays.asList('b', 'y', 'e'));
//        antRoutingGraphUtils.insert(antRoutingGraph, Arrays.asList('t', 'h', 'e', 'i', 'r'));
//        System.out.println(antRoutingGraph);
//
//    }
//
//    @Test
//    public void search() {
//
//        AntRoutingGraph antRoutingGraph = new AntRoutingGraph();
//        AntRoutingGraphUtils antRoutingGraphUtils = new AntRoutingGraphUtils();
//
//        antRoutingGraphUtils.insert(antRoutingGraph, Arrays.asList('t', 'h', 'e'));
//        antRoutingGraphUtils.insert(antRoutingGraph, Arrays.asList('a'));
//        antRoutingGraphUtils.insert(antRoutingGraph, Arrays.asList( 't', 'h', 'e', 'r', 'e'));
//        antRoutingGraphUtils.insert(antRoutingGraph, Arrays.asList( 'a', 'n', 's', 'w', 'e', 'r'));
//        antRoutingGraphUtils.insert(antRoutingGraph, Arrays.asList('a', 'n', 'y'));
//        antRoutingGraphUtils.insert(antRoutingGraph, Arrays.asList('b', 'y'));
//        antRoutingGraphUtils.insert(antRoutingGraph, Arrays.asList('b', 'y', 'e'));
//        antRoutingGraphUtils.insert(antRoutingGraph, Arrays.asList('t', 'h', 'e', 'i', 'r'));
//
//        Assert.assertEquals(true, antRoutingGraphUtils.search(antRoutingGraph, Arrays.asList('t', 'h', 'e', 'i', 'r')));
//        Assert.assertEquals(false, antRoutingGraphUtils.search(antRoutingGraph, Arrays.asList('t', 'h', 'e', 'i')));
//        Assert.assertEquals(true, antRoutingGraphUtils.search(antRoutingGraph, Arrays.asList('a', 'n', 'y')));
//        Assert.assertEquals(false, antRoutingGraphUtils.search(antRoutingGraph, Arrays.asList('b', 'y', 'e', 'e')));
//    }
//
//    @Test
//    public void delete() {
//
//        AntRoutingGraph antRoutingGraph = new AntRoutingGraph();
//        AntRoutingGraphUtils antRoutingGraphUtils = new AntRoutingGraphUtils();
//
//        antRoutingGraphUtils.insert(antRoutingGraph, Arrays.asList('t', 'h', 'e'));
//        antRoutingGraphUtils.insert(antRoutingGraph, Arrays.asList('a'));
//        antRoutingGraphUtils.insert(antRoutingGraph, Arrays.asList( 't', 'h', 'e', 'r', 'e'));
//        antRoutingGraphUtils.insert(antRoutingGraph, Arrays.asList( 'a', 'n', 's', 'w', 'e', 'r'));
//        antRoutingGraphUtils.insert(antRoutingGraph, Arrays.asList('a', 'n', 'y'));
//        antRoutingGraphUtils.insert(antRoutingGraph, Arrays.asList('b', 'y'));
//        antRoutingGraphUtils.insert(antRoutingGraph, Arrays.asList('b', 'y', 'e'));
//        antRoutingGraphUtils.insert(antRoutingGraph, Arrays.asList('t', 'h', 'e', 'i', 'r'));
//
//        System.out.println("Testing delete :");
//        System.out.println("Before deleting : " + antRoutingGraphUtils.search(antRoutingGraph, Arrays.asList('t', 'h', 'e', 'i', 'r')));
//        antRoutingGraphUtils.delete(antRoutingGraph, Arrays.asList('t', 'h', 'e', 'i', 'r'));
//        System.out.println("After deleting : " + antRoutingGraphUtils.search(antRoutingGraph, Arrays.asList('t', 'h', 'e', 'i', 'r')));
//
//        // Assertions.assertEquals('w', antRoutingGraphUtils.search(antRoutingGraph, 'w'));
//    }
}
